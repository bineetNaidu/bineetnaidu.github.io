export interface ProjectTypes {
  id?: string;
  name: string;
  url: string;
  link: string;
  github?: string;
  description: string;
  idx: number | string;
}

export type Techs = {
  id: string;
  name: string;
  image: string;
};
